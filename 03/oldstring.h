#ifndef OLDSTRING_H
#define OLDSTRING_H
#include <string>

double mean(const char *ptr);
double stdDev(const char *ptr, double mean);
std::string getWord(const char *ptr);

class oldstring
{
    public:
        oldstring();
    protected:
    private:
};

#endif // OLDSTRING_H
